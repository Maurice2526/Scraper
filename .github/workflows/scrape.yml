name: Scrape Room Data

# Läuft jede Stunde und kann manuell gestartet werden
on:
  schedule:
    - cron: '0 * * * *'  # jede Stunde
  workflow_dispatch:

jobs:
  scrape:
    runs-on: ubuntu-latest

    steps:
      # Repository auschecken
      - name: Repo auschecken
        uses: actions/checkout@v3

      # Node.js installieren
      - name: Node.js installieren
        uses: actions/setup-node@v3
        with:
          node-version: '20'

      # Abhängigkeiten installieren
      - name: Abhängigkeiten installieren
        run: npm install node-fetch@2

      # Scraper ausführen
      - name: Scraper ausführen
        run: node scraper.js

      # Änderungen committen und pushen (rooms.json wird immer überschrieben)
      - name: Änderungen committen und pushen
        env:
          PAT_TOKEN: ${{ secrets.PAT_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Alles für Commit vorbereiten
          git add rooms.json
          git commit -m "Automatisch aktualisierte rooms.json" || echo "Keine Änderungen"

          # Force Push auf main, überschreibt alles remote mit diesem Commit
          git push --force-with-lease https://x-access-token:$PAT_TOKEN@github.com/${{ github.repository }}.git HEAD:main
